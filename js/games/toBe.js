/* ==========================================================================
   TO BE — STUB MODULE
   Registered so the Learning Map can link to it and the engine's
   "coming soon" screen displays correctly. Implement `init(container,
   onFinish)` the same way flashcards.js does when this game is built.
   Data source: js/data.js -> `toBe` array.
   ========================================================================== */
games.toBe = {
  title: "To Be Challenge",
  // init(container, onFinish) { ... } — to be implemented
};
