/* ==========================================================================
   SENTENCE BUILDER — STUB MODULE
   Data source: js/data.js -> `sentences` array.
   Implement `init(container, onFinish)` following flashcards.js's pattern
   when this game is built (drag/tap words into correct order).
   ========================================================================== */
games.sentenceBuilder = {
  title: "Sentence Builder",
  // init(container, onFinish) { ... } — to be implemented
};
